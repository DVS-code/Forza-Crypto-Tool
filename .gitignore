# Build output
bin/
obj/
release/
*.user

# Decrypted game data and saves — never commit game content or profile data.
*.slt
*.sqlite
*_decrypted.*
*.decrypted.*
C_ProfileData*
*ProfileBackup*

# Local IV tables. Not secret in themselves (useless without the key), but they are
# large captured research artifacts that belong with the RE workspace, not in this repo.
m22_iv_table*.json

# Backups produced by FileSafety.ReplaceWithBackup
*.bak
*.CLEANBAK*

# Local archives / scratch
Archive.zip
